# Command File Templates for Expert System
#
# Replace {{DOMAIN}}, {{SCOPE}}, {{EXPERTISE_PATH}}, and {{ALLOWED_TOOLS}}
# for each generated command file.
#
# ═══════════════════════════════════════════════════════════════════════════
# FILE 1: plan.md
# ═══════════════════════════════════════════════════════════════════════════

---
name: {{DOMAIN}}-plan
allowed-tools: Read, SlashCommand, TodoWrite, Grep, Glob, Bash
description: Create implementation plans for {{SCOPE}}. Loads domain expertise before delegating to /plan to ensure {{DOMAIN}}-specific patterns, file paths, and constraints are respected. Use when planning changes to {{DOMAIN}}.
argument-hint: [user_request]
---

# Purpose

You are a Higher Order Prompt (HOP) that creates implementation plans with {{DOMAIN}} expertise as the primary focus. It reads the {{DOMAIN}} expertise file and critical related files before delegating to `/plan`, ensuring the planning process is informed by deep domain knowledge.

## Variables

USER_REQUEST: $1
EXPERTISE_FILE: {{EXPERTISE_PATH}}

## Instructions

- This is a two-step wrapper: load expertise context, then delegate to `/plan`
- The expertise file is your **mental model** — treat it as authoritative but verify against the codebase
- Let `/plan` handle the actual planning logic while you provide domain context
- Prioritize {{DOMAIN}}-specific elements of the request

## Workflow

1. **Load Expertise Context**
   - Read EXPERTISE_FILE to understand architecture, key files, and patterns
   - Identify files in the expertise doc that are directly relevant to USER_REQUEST
   - Read those files to confirm current implementation state
   - Note any patterns, gotchas, or integration points that apply to USER_REQUEST

2. **Execute Planning with Context**
   - Call `/plan` with USER_REQUEST as the argument
   - Planning will now be informed by the {{DOMAIN}} expertise loaded above

## Report

The report will be generated by the `/plan` command.


# ═══════════════════════════════════════════════════════════════════════════
# FILE 2: question.md
# ═══════════════════════════════════════════════════════════════════════════

---
name: {{DOMAIN}}-question
allowed-tools: Read, Bash, TodoWrite, Grep, Glob
description: Answer questions about {{SCOPE}} — architecture, patterns, key files, and implementation details — without making code changes. Use when asking how {{DOMAIN}} works, where things live, or why a pattern exists.
argument-hint: [question]
---

# {{DOMAIN}} Expert — Question Mode

Answer the user's question by analyzing the {{DOMAIN}} implementation. This is a read-only mode: DO NOT write, edit, or create any files.

## Variables

USER_QUESTION: $1
EXPERTISE_PATH: {{EXPERTISE_PATH}}

## Instructions

- IMPORTANT: Read-only. Zero file writes.
- If the question requires code changes, explain what would need to be done conceptually — do not implement
- Validate expertise claims against the real codebase before answering
- Include file paths and line numbers in your answer where relevant

## Workflow

1. Read EXPERTISE_PATH to understand {{DOMAIN}} architecture and patterns
2. Identify which parts of the expertise are relevant to USER_QUESTION
3. Read the relevant source files to validate and enrich the answer
4. Respond per the Report format

## Report

- **Direct answer** to USER_QUESTION
- **Supporting evidence** from EXPERTISE_PATH and the codebase (file:line references)
- **Conceptual explanation** of the relevant architecture or pattern
- **Diagrams** where they clarify relationships (ASCII or Mermaid)


# ═══════════════════════════════════════════════════════════════════════════
# FILE 3: self-improve.md
# ═══════════════════════════════════════════════════════════════════════════

---
name: {{DOMAIN}}-self-improve
allowed-tools: Read, Grep, Glob, Bash, Edit, Write, TodoWrite
description: Validate and update the {{DOMAIN}} expertise file against the actual codebase. Detects outdated file paths, missing functions, changed patterns, and new files. Run after any {{DOMAIN}} changes to keep expertise accurate.
argument-hint: [check_git_diff (true/false)] [focus_area (optional)]
---

# Purpose

Maintain the {{DOMAIN}} expertise file's accuracy by comparing it against the actual codebase. The expertise file is the **mental model** for all {{DOMAIN}} tasks — it must reflect reality, not aspirations.

## Variables

CHECK_GIT_DIFF: $1    # default: false
FOCUS_AREA: $2        # optional — specific area to prioritize
EXPERTISE_FILE: {{EXPERTISE_PATH}}
MAX_LINES: 1000

## Workflow

1. **Check Git Diff (conditional)**
   - If CHECK_GIT_DIFF is "true": run `git diff` to identify recent {{DOMAIN}}-related changes
   - Note changed files for targeted validation in step 3

2. **Read Current Expertise**
   - Read EXPERTISE_FILE fully
   - Note any sections that seem potentially stale or incomplete

3. **Validate Against Codebase**
   - For each file listed in EXPERTISE_FILE: verify it exists and read its current content
   - Use Grep to verify all documented function names actually exist in the files listed
   - Check: data shapes, patterns, integration points, gotchas
   - If FOCUS_AREA is set, prioritize validation of that area

4. **Identify Discrepancies**
   - Missing files/functions in expertise (new code not documented)
   - Wrong paths or stale line numbers
   - Changed function signatures
   - Removed features still listed
   - Incorrect architectural descriptions

5. **Update Expertise File**
   - Fix all discrepancies
   - Add missing information
   - Remove obsolete information
   - Keep all file paths relative to project root
   - Maintain YAML structure

6. **Enforce Line Limit**
   - Run: `wc -l {{EXPERTISE_PATH}}`
   - If count > MAX_LINES: trim least-critical sections (verbose prose, redundant examples)
   - Repeat until ≤ MAX_LINES

7. **Validate YAML**
   - Run: `python3 -c "import yaml; yaml.safe_load(open('{{EXPERTISE_PATH}}'))"`
   - Fix any syntax errors

## Report

### Summary
- Git diff checked: yes/no
- Focus area: [value or none]
- Discrepancies found: N / remedied: N
- Final line count: N/1000

### Discrepancies Found
[List each: what was wrong → where correct info was found → how fixed]

### Updates Made
[Added / Updated / Removed sections]

### Line Limit
- Initial: N / Final: N / Trimming needed: yes/no

### Validation
- All key files verified: yes/no
- YAML syntax valid: yes/no
- Areas needing future attention: [list or none]


# ═══════════════════════════════════════════════════════════════════════════
# FILE 4: plan_build_improve.md
# ═══════════════════════════════════════════════════════════════════════════

---
name: {{DOMAIN}}-plan-build-improve
allowed-tools: Task, TaskOutput, TodoWrite
description: End-to-end {{DOMAIN}} implementation workflow. Chains expertise-informed planning → code build → expertise self-improvement. Use for complete feature development in {{SCOPE}}.
argument-hint: [implementation_request] [human_in_the_loop (bool)]
---

# Purpose

Orchestrates a complete {{DOMAIN}} implementation cycle by chaining three specialized commands: expertise-informed planning, building from the plan, and self-improving the expertise based on changes made.

## Variables

USER_PROMPT: $1
HUMAN_IN_THE_LOOP: $2    # default: true

## Instructions

- Execute steps 1–3 sequentially using the Task tool for each step
- Each subagent starts fresh — provide complete instructions in each Task prompt
- Use TaskOutput to retrieve results before proceeding to the next step
- DO NOT STOP between steps — complete the entire workflow

## Workflow

### Step 1: Create Plan

```
Task(
  subagent_type: "general-purpose",
  prompt: "Run SlashCommand('/experts:{{DOMAIN}}:plan [USER_PROMPT]'). Return the path to the generated plan file."
)
```

Use TaskOutput to get `path_to_plan` before proceeding.

### Step 2: Build

```
Task(
  subagent_type: "general-purpose",
  prompt: "Run SlashCommand('/implement [path_to_plan]'). Implement the entire plan. Return a summary of files changed."
)
```

Use TaskOutput to get `build_report` before proceeding.

### Step 3: Self-Improve

```
Task(
  subagent_type: "general-purpose",
  prompt: "Run SlashCommand('/experts:{{DOMAIN}}:self-improve true'). Return the self-improvement report."
)
```

Use TaskOutput to get `self_improve_report`.

### Step 4: Report

## Report

### Workflow Summary
- User request: [USER_PROMPT]
- Steps completed: 3/3

### Step 1: Planning
- Plan file: [path_to_plan]

### Step 2: Build
- [build_report summary]

### Step 3: Self-Improve
- [self_improve_report summary]

### Final Status
{{DOMAIN}} implementation workflow complete.
